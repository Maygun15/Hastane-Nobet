# 📊 Sistem Mimarisi ve Akış Diyagramları

## 1. GENEL SİSTEM ARKİTEKTÜRÜ

```
┌─────────────────────────────────────────────────────────────────┐
│                    USER INTERFACE LAYER                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │  Planning    │  │  Calendar    │  │  Timeline    │          │
│  │    List      │  │    View      │  │    View      │          │
│  └──────────────┘  └──────────────┘  └──────────────┘          │
│         │                  │                  │                 │
│         └──────────────────┼──────────────────┘                 │
│                            ▼                                    │
│            ┌─────────────────────────────────┐                 │
│            │   PlanningPage Component        │                 │
│            │   (State Management + Logic)    │                 │
│            └─────────────────────────────────┘                 │
│                            ▼                                    │
│            ┌─────────────────────────────────┐                 │
│            │    API Service + Custom Hooks   │                 │
│            │  (usePlanning, useTasks)        │                 │
│            └─────────────────────────────────┘                 │
│                            │                                    │
│                            ▼ HTTP/CORS                         │
└────────────────────────────┼───────────────────────────────────┘
                             │
                   ┌─────────▼──────────┐
                   │  NETWORK/INTERNET  │
                   └─────────┬──────────┘
                             │
┌────────────────────────────▼───────────────────────────────────┐
│                    SERVER-SIDE LAYER                           │
│            ┌─────────────────────────────────┐                │
│            │     Express.js Server           │                │
│            │   (Port 5000)                   │                │
│            └──────────┬──────────────────────┘                │
│                       ▼                                       │
│        ┌──────────────────────────────────┐                  │
│        │   CORS Middleware                │                  │
│        │   JSON Parser                    │                  │
│        │   Request Logger                 │                  │
│        └──────────────────────────────────┘                  │
│                       ▼                                       │
│    ┌─────────────────────────────────────────┐              │
│    │         API ENDPOINTS                   │              │
│    │  ┌──────────────┐  ┌──────────────┐    │              │
│    │  │  Planning    │  │    Tasks     │    │              │
│    │  │ Endpoints    │  │  Endpoints   │    │              │
│    │  └──────────────┘  └──────────────┘    │              │
│    └─────────────────────────────────────────┘              │
│                       ▼                                       │
│        ┌──────────────────────────────────┐                  │
│        │   Business Logic & Validation    │                  │
│        └──────────────────────────────────┘                  │
│                       ▼                                       │
│        ┌──────────────────────────────────┐                  │
│        │   Database Layer                 │                  │
│        │   (Mock / MongoDB / PostgreSQL)  │                  │
│        └──────────────────────────────────┘                  │
│                       ▼                                       │
│        ┌──────────────────────────────────┐                  │
│        │        Data Storage              │                  │
│        │   Plannings & Tasks              │                  │
│        └──────────────────────────────────┘                  │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. REQUEST/RESPONSE FLOW

### Başarılı Request Akışı

```
CLIENT (Browser)                          SERVER (Express)
     │                                           │
     │  1. User Click Event                     │
     │  (Click "Create Planning")               │
     │                                          │
     ├─► 2. Form Data Validation              │
     │     (Frontend)                           │
     │                                          │
     ├─► 3. Generate Request ID                │
     │     X-Request-ID: 123-abc                │
     │                                          │
     ├─► 4. Log Request                        │
     │     📤 API Request: POST /api/v1...      │
     │                                          │
     ├──────────────────────────────────────► 5. CORS Check
     │                                          │
     │          6. Parse JSON Body             ◄──
     │                                          │
     │          7. Validate Input              │
     │                                          │
     │          8. Log Validation              │
     │                                          │
     │          9. Check Data Format           │
     │                                          │
     │          10. Save to Database           │
     │                                          │
     │          11. Generate Response          │
     │    {                                     │
     │     "success": true,                     │
     │     "statusCode": 201,                   │
     │     "data": { _id: 1, ... }              │
     │    }                                     │
     │                                          │
     ├◄────────────────────────────────────── 12. Send Response
     │                                          │
     ├─► 13. Log Response                      │
     │     📥 API Response: 201 Success         │
     │                                          │
     ├─► 14. Axios Interceptor                │
     │     Check success flag                  │
     │                                          │
     ├─► 15. Hook (createPlanning)            │
     │     Update state                        │
     │                                          │
     ├─► 16. Re-render Component               │
     │     Show new planning                   │
     │                                          │
     └─► 17. Show Toast                       │
          "Planning Created!"                  │
```

---

## 3. ERROR HANDLING FLOW

```
CLIENT (Browser)                          SERVER (Express)
     │                                          │
     ├──────────────────────────────────────► Request
     │                                          │
     │                                    1. Check Data
     │                                          │
     │                              2. Validation Failed ✗
     │                                          │
     │                              3. Create Error Response:
     │                               {
     │                                 "success": false,
     │                                 "statusCode": 400,
     │                                 "error": {
     │                                   "code": "VALIDATION_ERROR",
     │                                   "message": "Title required",
     │                                   "details": [{...}]
     │                                 }
     │                               }
     │                                          │
     ├◄────────────────────────────────────── Response
     │                                          │
     ├─► 4. Axios Error Interceptor
     │                                          │
     ├─► 5. Handle Error:
     │     - Type: VALIDATION_ERROR
     │     - StatusCode: 400
     │     - Details: {...}
     │                                          │
     ├─► 6. Check Error Type
     │                                          │
     └─► 7. Show Error Message
          "Title is required"
          (Form'a hata göster)
```

---

## 4. DATA FLOW - PLANLAMA OLUŞTURMA

```
┌─────────────────────────────────────────────────────────┐
│                    USER INTERACTION                     │
│           Click "Add Planning" Button                   │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│                  FORM COMPONENT                         │
│  ┌────────────────────────────────────────────────┐    │
│  │  State:                                        │    │
│  │  - formData: { title, startDate, endDate... } │    │
│  │  - errors: {}                                 │    │
│  │  - isLoading: false                           │    │
│  └────────────────────────────────────────────────┘    │
└────────────────┬────────────────────────────────────────┘
                 │ On Submit
                 ▼
┌─────────────────────────────────────────────────────────┐
│            FORM VALIDATION (Frontend)                  │
│  ✓ Title tidak kosong                                 │
│  ✓ Start date < End date                              │
│  ✓ Priority valid                                     │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│         API SERVICE CALL                               │
│  planningService.create({                              │
│    title: "Q1 2026",                                   │
│    startDate: "2026-03-01",                            │
│    endDate: "2026-03-31",                              │
│    priority: "high"                                    │
│  })                                                    │
└────────────────┬────────────────────────────────────────┘
                 │ axios.post()
                 ▼
         ┌───────────────────┐
         │   HTTP REQUEST    │
         │  POST /api/v1/    │
         │   planning        │
         └────────┬──────────┘
                  │ (CORS enabled)
                  ▼
┌─────────────────────────────────────────────────────────┐
│          EXPRESS BACKEND                               │
│  app.post('/api/v1/planning', (req, res) => {          │
│                                                        │
│    // 1. Validation                                   │
│    const errors = validatePlanning(req.body);         │
│    if (errors) return sendResponse(400, errors);      │
│                                                        │
│    // 2. Create Object                                │
│    const planning = {                                 │
│      _id: 1,                                          │
│      title: req.body.title,                           │
│      status: 'draft',                                 │
│      ...                                              │
│    };                                                 │
│                                                        │
│    // 3. Save (Mock Database)                        │
│    mockDatabase.plannings.push(planning);             │
│                                                        │
│    // 4. Send Response                               │
│    sendResponse(201, true, planning,                  │
│      'Planlama oluşturuldu')                          │
│                                                        │
│  })                                                   │
└────────────────┬────────────────────────────────────────┘
                 │ HTTP Response (201)
                 ▼
         ┌───────────────────┐
         │  HTTP RESPONSE    │
         │  {                │
         │   success: true,  │
         │   data: {...},    │
         │   statusCode: 201 │
         │  }                │
         └────────┬──────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────────┐
│       FRONTEND RESPONSE HANDLING                       │
│                                                        │
│  // Interceptor check                                │
│  if (response.data.success) {                        │
│    // Success path                                  │
│    return result;                                   │
│  } else {                                           │
│    // Error path                                   │
│    throw error;                                     │
│  }                                                 │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│          HOOK STATE UPDATE                            │
│  setPlannings(prev => [newPlanning, ...prev])        │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│          COMPONENT RE-RENDER                          │
│  PlanningCard göster yeni planlama ile               │
└────────────────┬────────────────────────────────────────┘
                 │
                 ▼
┌─────────────────────────────────────────────────────────┐
│           USER FEEDBACK                               │
│  Toast: "Planning created successfully!" ✓           │
│  Form kapan                                          │
│  Loading state kapat                                 │
└─────────────────────────────────────────────────────────┘
```

---

## 5. COMPONENT HIERARCHY

```
PlanningPage (Main Container)
│
├─ Header
│  └─ "New Planning" Button
│
├─ Toast Notification
│
├─ Toolbar
│  ├─ View Toggle (List/Calendar/Timeline)
│  ├─ Search Input
│  ├─ Status Filter
│  └─ Priority Filter
│
├─ PlanningForm (conditional)
│  ├─ Title Input
│  ├─ Description Textarea
│  ├─ Date Inputs
│  ├─ Priority Select
│  └─ Submit/Cancel Buttons
│
└─ Content Area (based on view)
   │
   ├─ LIST VIEW
   │  ├─ Planning Column
   │  │  ├─ PlanningCard (map)
   │  │  │  ├─ Title
   │  │  │  ├─ Status Badge
   │  │  │  ├─ Priority Badge
   │  │  │  ├─ Progress Bar
   │  │  │  └─ Actions
   │  │  │
   │  │  └─ (repeat for each planning)
   │  │
   │  └─ Details Column
   │     ├─ Planning Details
   │     ├─ TaskForm (conditional)
   │     └─ Task List
   │        ├─ TaskItem (map)
   │        │  ├─ Checkbox
   │        │  ├─ Title
   │        │  ├─ Priority Badge
   │        │  ├─ Dates
   │        │  └─ Actions
   │        │
   │        └─ (repeat for each task)
   │
   ├─ CALENDAR VIEW
   │  ├─ Calendar Grid
   │  │  └─ Day Cell (map)
   │  │     ├─ Date Number
   │  │     └─ Events
   │  │
   │  └─ Upcoming Tasks Sidebar
   │     └─ Task List (sorted by due date)
   │
   └─ TIMELINE VIEW
      └─ Timeline Items (map)
         ├─ Planning Title
         ├─ Date Range
         ├─ Progress Bar
         └─ (repeat for each planning)
```

---

## 6. STATE MANAGEMENT FLOW

```
PlanningPage Component State:
│
├─ view: 'list' | 'calendar' | 'timeline'
│
├─ selectedPlanning: Planning | null
│
├─ showPlanningForm: boolean
│  └─ editingPlanning: Planning | null
│
├─ showTaskForm: boolean
│  └─ editingTask: Task | null
│
├─ filters: {
│    status: '',
│    priority: '',
│    searchTerm: ''
│  }
│
└─ toast: {
   message: string,
   type: 'success' | 'error' | 'warning' | 'info'
 } | null

API Hooks State:
│
├─ usePlanning() Hook
│  ├─ plannings: Planning[]
│  ├─ loading: boolean
│  ├─ error: Error | null
│  └─ methods: fetch, create, update, delete
│
└─ useTasks() Hook
   ├─ tasks: Task[]
   ├─ loading: boolean
   ├─ error: Error | null
   └─ methods: fetch, create, update, updateStatus, delete
```

---

## 7. API REQUEST TYPES

```
CREATE FLOW:
User → Form → API Service → Axios → Backend → Validation → Database → Response → Hook → State → Render

UPDATE FLOW:
User → Item Click → Form → API Service → Axios → Backend → Validation → Database → Response → Hook → State → Render

DELETE FLOW:
User → Confirm → API Service → Axios → Backend → Database → Response → Hook → State → Render

LIST FLOW:
Component Mount → Hook → API Service → Axios → Backend → Database → Response → Hook → State → Render

STATUS CHANGE FLOW:
Checkbox Click → API Service → Axios → Backend → Update → Response → Hook → State → Render
```

---

## 8. ERROR HANDLING TREE

```
Request Error
│
├─ Network Error
│  ├─ No Internet
│  ├─ Backend Down
│  └─ DNS Resolution Failed
│
├─ CORS Error
│  ├─ Origin Not Allowed
│  └─ Method Not Allowed
│
├─ HTTP Status Error
│  ├─ 400 Bad Request
│  │  └─ Validation Error
│  │     └─ Show field errors in form
│  │
│  ├─ 404 Not Found
│  │  └─ Resource doesn't exist
│  │
│  ├─ 401 Unauthorized
│  │  └─ Need to login
│  │
│  └─ 500 Internal Server Error
│     └─ Backend issue
│
└─ Custom Error
   ├─ Unknown error
   └─ Show generic message
```

---

## 9. PERFORMANCE CONSIDERATIONS

```
┌──────────────────────────────────────────┐
│      OPTIMIZATION STRATEGIES              │
├──────────────────────────────────────────┤
│                                          │
│  ✓ Pagination (List göster limit ile)   │
│  ✓ Lazy Loading (Component Level)       │
│  ✓ Memoization (React.memo)             │
│  ✓ Caching (Recently fetched data)      │
│  ✓ Debouncing (Search/Filter)           │
│  ✓ Code Splitting (Route based)         │
│  ✓ Image Optimization (CDN)             │
│  ✓ Compression (gzip on server)         │
│                                          │
└──────────────────────────────────────────┘
```

---

## 10. DEPLOYMENT ARCHITECTURE

```
┌──────────────────────────────────────────────────────────┐
│                    PRODUCTION SETUP                      │
├──────────────────────────────────────────────────────────┤
│                                                          │
│  ┌─────────────┐      ┌──────────────┐                 │
│  │  CDN (Edge) │      │  Load Balancer│               │
│  │  Static     │      │  (Traffic)    │               │
│  │  Assets     │      └────────┬──────┘               │
│  └─────────────┘               │                      │
│                                │                      │
│         ┌──────────────────────┼──────────────────┐   │
│         │                      │                  │   │
│         ▼                      ▼                  ▼   │
│    ┌────────┐            ┌────────┐         ┌────────┐
│    │Backend │ Cluster    │Backend │ Node    │Backend │
│    │Server  │            │Server  │         │Server  │
│    │(Auto  │            │        │         │        │
│    │Scaling)             └────────┘         └────────┘
│    └──┬─────┘                 │                 │    │
│       │                       │                 │    │
│       └───────────┬───────────┼────────────────┘    │
│                   │           │                     │
│                   ▼           ▼                     │
│              ┌─────────────────────┐               │
│              │   Redis Cache       │               │
│              │   (Sessions, Data)  │               │
│              └────────────┬────────┘               │
│                           │                       │
│                           ▼                       │
│              ┌─────────────────────┐              │
│              │   Primary Database  │              │
│              │   (MongoDB/PG)      │              │
│              └────────────┬────────┘              │
│                           │                      │
│                           ▼                      │
│              ┌─────────────────────┐             │
│              │  Database Replicas  │             │
│              │  (Backup/Failover)  │             │
│              └─────────────────────┘             │
│                                                  │
└──────────────────────────────────────────────────┘
```

---

**Versiyon:** 1.0  
**Güncelleme:** 2026-02-27  
**Amaç:** Visual system understanding
